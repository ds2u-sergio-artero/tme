# aia-harness
.claude/
dist

# secrets — never commit
.env
.env.*
!.env.example

# local / generated
.claude/worktrees/
.claude/*.graphify-bak
graphify-out/
node_modules/
.superpowers/

# vault
vault

# obsidian-mcp server (clone + .env + docker volumes) — never commit
.obsidian-mcp/
